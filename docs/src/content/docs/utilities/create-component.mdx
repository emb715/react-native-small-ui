---
title: Create Component
sidebar: {
  order: 0
}
---

import { Card, CardGrid, Code, Tabs, TabItem, LinkButton, Aside } from '@astrojs/starlight/components';

export const exampleCode = `
import { createComponent } from 'react-native-small-ui';
import { View } from 'react-native';

const MyComponent = createComponent(View, {
  alignItems: 'center',
  justifyContent: 'center',
  _light: {
    backgroundColor: '#f1f1f1',
  },
  _dark: {
    backgroundColor: '#123123',
  },
  _ios: {
    marginTop: 10,
  },
  _android: {
    marginTop: 16,
  },
  _web: {
    marginTop: 0
  }
})
`

## Overview

`createComponent` is the core utility that wraps React Native components with enhanced styling capabilities including platform-specific styles, color mode support, and TypeScript autocomplete.

## Installation

```js
import { createComponent } from 'react-native-small-ui';
```

This is part of the core package (~15KB) and has no heavy dependencies.

---

<Aside type="caution" title="Performance Warning">
**Always create components OUTSIDE the render cycle.**

Creating components inside function components causes React to create a new component type on every render, breaking reconciliation and causing unnecessary re-mounts.

**❌ Wrong:**
```jsx
function MyComponent() {
  const Box = createComponent(View, { padding: 16 }); // New component every render!
  return <Box />;
}
```

**✅ Correct:**
```jsx
const Box = createComponent(View, { padding: 16 }); // Created once

function MyComponent() {
  return <Box />;
}
```

For dynamic styling, use **props** instead of creating new components:
```jsx
const Box = createComponent(View, {});

function MyComponent({ theme }) {
  return <Box padding={theme.space[4]} />; // Dynamic via props
}
```
</Aside>

---

## Basic Usage

You can use the `createComponent` helper function to extend the styling capabilities to platform specifics, color mode, and inline style with props.


```
createComponent(Component, styleObject, defaultProps)
```


#### Utility Style Props

All available styles of the component. Like: _backgroundColor_, _alignContent_, etc.

Customized are prefixed with **underscore** _
- `_light`
- `_dark`
- `_ios`
- `_android`
- `_web`


Example: 
<Code code={exampleCode} lang="js" title='MyComponent'/>


<Aside type="note">
The new created component `MyComponent` will also extend the same props for styling.
</Aside>

--- 

```typescript
function App() {
  const myFlag = useMemo(() => {
    // something that will return true or false
    return true;
  });

  return (
    <MyComponent
      paddingTop={myFlag ? 16 : 0}
      _web={{
        paddingTop: myFlag ? 8 : undefined
      }}
    >
      <Text>Hi!</Text>
    </MyComponent>
  );
}
```
